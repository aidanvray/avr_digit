start = root
root = element Ozymandias {layer}
layer = element layer {agent , mixed { (lb | layer | ellipsis)* }}
agent = attribute  agent {text}
lb = element lb {n}
n = attribute n {xsd:integer}
ellipsis = element ellipsis {rend}
rend = attribute rend {text}